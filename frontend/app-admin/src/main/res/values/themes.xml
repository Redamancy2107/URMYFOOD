<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.URMYFOOD" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.URMYFOOD" parent="Base.Theme.URMYFOOD" />

    <style name="Theme.URMYFOOD.Launcher" parent="Theme.URMYFOOD">
        <item name="android:windowBackground">@drawable/splash_gradient_bg</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="CircleImageAppearance">
        <item name="cornerSize">50%</item>
    </style>
</resources>